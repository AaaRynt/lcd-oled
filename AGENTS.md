请在当前项目中创建一个介绍并对比 LCD 与 OLED 屏幕技术的中文单页网站。

技术栈：

- React
- Tailwind CSS
- lucide-react
- 不使用其他 UI 组件库
- 不使用外部图片
- 不引入图表库
- 使用 CSS、渐变、网格和 React 组件自行构建视觉效果

页面目标：
这是一个面向普通消费者和数码爱好者的屏幕技术科普页面。页面既要解释 LCD 和 OLED 的工作原理，也要帮助用户根据实际使用场景选择屏幕。

整体风格：

- 现代、克制、偏科技媒体或产品介绍页
- 不要做成企业后台、课程 PPT 或普通博客
- 使用深色背景，主体背景接近 zinc-950
- LCD 使用冷蓝色作为视觉标识
- OLED 使用紫色或品红色作为视觉标识
- 可以使用少量半透明卡片、细边框、网格背景和柔和光晕
- 不要堆积大量强烈渐变
- 保持足够留白
- 最大内容宽度约 1200px
- 桌面端和移动端均需良好适配
- 所有交互元素需要有 hover、focus-visible 状态
- 支持 prefers-reduced-motion

请将页面拆分为合理的 React 组件，避免所有代码都堆在 App.jsx 中。

建议组件：

- Header
- HeroSection
- DisplayDemo
- PrincipleSection
- ComparisonSection
- ScenarioSection
- RecommendationSection
- FAQSection
- Footer

页面内容如下。

一、顶部导航

左侧品牌名称：
「Display Lab」

导航项：

- 工作原理
- 核心差异
- 使用场景
- 如何选择

右侧放置一个带 Monitor 图标的状态标签：
「LCD vs OLED」

导航栏使用 sticky 定位，并带有半透明背景和 backdrop-blur。

二、Hero 首屏

小标签：
「DISPLAY TECHNOLOGY GUIDE」

主标题：
「LCD 与 OLED，差别不只在黑色更黑」

说明文字：
「它们使用完全不同的发光方式，也因此在对比度、亮度、响应速度、功耗、寿命和价格方面表现不同。没有绝对更好的屏幕，只有更适合具体设备和使用方式的方案。」

按钮：

1. 「查看核心差异」
2. 「帮我选择」

右侧或下方制作一个可交互的屏幕演示组件。

演示组件要求：

- 使用一个左右拖动的对比滑块
- 左侧模拟 LCD，右侧模拟 OLED
- 两侧展示相同的夜景或抽象界面，不使用图片，可以用 CSS 绘制：
  - 深色天空
  - 月亮
  - 几个发光窗口
  - 文本和 UI 元素
- LCD 一侧的黑色略微发灰，并表现出轻微背光效果
- OLED 一侧的黑色接近纯黑，亮部更加突出
- 组件底部标注：
  - LCD：背光始终存在
  - OLED：像素独立发光
- 支持鼠标拖动和触摸拖动
- 使用 aria-label，保证基本可访问性

Hero 下方放置三个简短事实：

「发光方式」
LCD 依赖背光，OLED 像素自发光

「黑色表现」
OLED 可以关闭单个像素

「选择逻辑」
使用场景比参数排名更重要

三、工作原理

标题：
「两种屏幕，是两套完全不同的发光逻辑」

左侧 LCD 卡片：

标题：
「LCD：用液晶控制背光」

正文：
「LCD 面板本身不会发光。屏幕后方的背光层持续提供光线，液晶层负责控制光线是否通过，再经过彩色滤光片形成最终画面。」

流程展示：
背光层 → 液晶层 → 彩色滤光片 → 人眼

特点列表：

- 背光通常覆盖整个屏幕或多个分区
- 黑色画面仍可能有少量光线通过
- 技术成熟，成本和供应链相对稳定

使用 lucide 图标：
Lightbulb、Layers3、PanelTop

右侧 OLED 卡片：

标题：
「OLED：每个像素自己发光」

正文：
「OLED 不需要独立背光层。每个像素都可以单独发光、调节亮度或彻底关闭，因此可以获得接近纯黑的画面和极高的明暗对比。」

流程展示：
电流 → 有机发光材料 → 像素直接发光 → 人眼

特点列表：

- 每个像素可以独立控制
- 显示黑色时可直接关闭像素
- 面板可以做得更薄，也更容易实现柔性形态

使用 lucide 图标：
Sparkles、Grid2X2、Zap

在两张卡片中加入简单的像素结构示意图：

- LCD 显示背光层和 RGB 滤光层
- OLED 显示独立发光的 RGB 像素
- 只使用 div 和 Tailwind CSS，不使用 SVG 图片

四、核心差异

标题：
「真正影响体验的，不只是画质」

说明：
「以下结论描述的是两类技术的常见特征。具体表现仍会受到面板型号、调校方式、亮度策略和设备设计影响。」

创建响应式对比表格。

对比项目与内容：

1. 发光方式
   LCD：统一背光经过液晶层调制
   OLED：每个像素独立发光

2. 黑色与对比度
   LCD：黑色通常会受到背光影响
   OLED：像素关闭后可呈现接近纯黑的效果

3. 全屏亮度
   LCD：高亮画面下通常更容易维持稳定亮度
   OLED：高亮面积增加时，部分设备会主动限制亮度

4. HDR 表现
   LCD：高端分区背光产品可以获得较强亮度
   OLED：暗部控制和局部对比更突出

5. 响应速度
   LCD：通常较慢，快速运动时更容易出现拖影
   OLED：像素响应通常更快

6. 可视角度
   LCD：不同面板类型差异较大
   OLED：通常拥有较宽的可视角度

7. 功耗
   LCD：功耗与背光亮度关系更直接
   OLED：功耗会明显受到画面内容影响，深色界面通常更省电

8. 烧屏风险
   LCD：基本没有 OLED 式永久残影问题
   OLED：长期显示固定高亮元素时存在老化不均风险

9. 频闪与调光
   LCD：部分产品使用 PWM，也有 DC 调光产品
   OLED：低亮度下常见 PWM 调光，但不同设备差异很大

10. 厚度与形态
    LCD：结构层数较多
    OLED：更薄，更容易制作柔性或折叠面板

11. 文本显示
    LCD：标准 RGB 子像素排列通常适合文本显示
    OLED：文本清晰度会受到像素密度和子像素排列影响

12. 成本
    LCD：成熟且价格跨度较大
    OLED：同等级尺寸下通常更昂贵，但价格正在逐渐下降

桌面端使用表格。
移动端将每一项变为独立对比卡片，不允许横向溢出。

五、交互式维度对比

制作一个维度切换组件，顶部放置五个按钮：

- 电影
- 游戏
- 办公
- 户外
- 长期使用

点击不同按钮时，展示 LCD 和 OLED 在该场景下的相对优劣。

不要使用虚假的精确测试数据，也不要展示类似“OLED 95 分、LCD 72 分”的伪科学评分。

采用以下形式：

- 两条相对表现条
- 文本解释
- 一个「需要注意」提示

具体文案：

电影：
LCD：
「高端 Mini LED LCD 可以提供较高峰值亮度，但暗场仍可能受到光晕影响。」
OLED：
「纯黑和像素级控光适合暗场电影，字幕周围也不容易出现背光光晕。」
需要注意：
「明亮客厅中，还应关注实际全屏亮度和屏幕反射率。」

游戏：
LCD：
「高刷新率产品选择丰富，长时间显示固定 HUD 的心理负担较低。」
OLED：
「响应速度快，运动画面清晰，暗部场景的对比度更突出。」
需要注意：
「刷新率、输入延迟和调校质量通常比面板名称本身更重要。」

办公：
LCD：
「标准 RGB 排列、稳定的静态界面表现和较低成本，使其依然适合长期办公。」
OLED：
「高对比度和轻薄设计体验优秀，但需要注意子像素排列与固定界面。」
需要注意：
「阅读文字时，应重点观察字体边缘、亮度舒适度和调光方式。」

户外：
LCD：
「部分高亮 LCD 可以持续维持较高亮度。」
OLED：
「高端 OLED 峰值亮度很强，但持续亮度取决于设备的温控和亮度策略。」
需要注意：
「屏幕反射率有时比实验室峰值亮度更影响户外可读性。」

长期使用：
LCD：
「面对固定工具栏、表格和监控界面时更加省心。」
OLED：
「现代设备拥有像素位移和亮度保护机制，但有机材料仍会随使用逐渐老化。」
需要注意：
「烧屏不是短时间必然发生，也不应被描述为完全不存在。」

六、使用场景

标题：
「根据你真正使用屏幕的方式来选择」

创建四张场景卡片。

1. 长时间办公与编程
   推荐倾向：LCD
   说明：
   「每天长时间显示编辑器、浏览器标签栏、菜单栏和固定工具栏时，LCD 通常更加省心。文字表现还应关注分辨率和子像素排列。」
   标签：
   「静态界面」「文字阅读」「长期使用」
   图标：
   Code2

2. 电影与暗场内容
   推荐倾向：OLED
   说明：
   「关闭环境光观看电影时，OLED 的纯黑和像素级控光能够明显提升暗场层次和沉浸感。」
   标签：
   「纯黑」「高对比度」「暗场」
   图标：
   Film

3. 高频游戏
   推荐倾向：根据具体产品选择
   说明：
   「OLED 通常拥有更快响应速度，但高刷新率 LCD 也可能拥有优秀的输入延迟、亮度和价格表现。」
   标签：
   「高刷新率」「响应速度」「输入延迟」
   图标：
   Gamepad2

4. 户外或高亮环境
   推荐倾向：比较实际设备
   说明：
   「不要只看 OLED 或 LCD 标签。持续亮度、屏幕反射率、自动亮度策略和设备温控都会影响实际可读性。」
   标签：
   「持续亮度」「反射率」「温控」
   图标：
   Sun

七、选择建议

标题：
「你可能更适合哪一种？」

设计一个简易选择器，依次询问四个问题：

1. 你是否经常长时间显示固定界面？
2. 你是否特别在意纯黑和暗场表现？
3. 你是否经常在明亮环境中使用屏幕？
4. 你是否愿意为更好的对比度支付更高价格？

每题使用两个按钮：
「是」和「否」

完成后展示建议结果，但不能给出绝对结论。

结果文案可以根据回答动态组合：

偏向 LCD：
「你可能更适合 LCD。你的使用方式更看重长时间静态显示、稳定亮度或成本控制。不过，具体购买时仍应比较面板类型、分辨率、色彩和调光方式。」

偏向 OLED：
「你可能更适合 OLED。你更看重暗场画质、像素响应速度和轻薄形态。不过，长期固定界面、调光方式和实际亮度仍值得关注。」

结果接近：
「你的需求没有明显偏向。此时具体产品的调校、亮度、刷新率、分辨率和价格，可能比 LCD 或 OLED 的技术标签更重要。」

提供「重新选择」按钮。

使用 Gauge、CheckCircle2、RotateCcw 图标。

八、常见误区

标题：
「关于屏幕技术的几个常见误区」

使用 accordion 手风琴交互，默认展开第一项。

问题 1：
「OLED 一定比 LCD 更护眼吗？」

答案：
「不能仅根据面板类型判断。观看舒适度还会受到亮度、环境光、色温、反射率、PWM 频率、调光深度和个人敏感程度影响。OLED 可能拥有更好的对比度，但部分产品在低亮度下的 PWM 调光也可能让敏感用户感到不适。」

问题 2：
「OLED 一定会烧屏吗？」

答案：
「OLED 像素会随使用逐渐老化，长期显示固定且高亮的内容可能造成老化不均。但是否出现明显烧屏，与累计使用时间、亮度、内容类型、散热和保护机制都有关系，不能简单理解为短时间必然损坏。」

问题 3：
「Mini LED 是不是 OLED？」

答案：
「不是。Mini LED 通常仍然属于 LCD，它使用尺寸更小、数量更多的 LED 作为背光，从而实现更多局部调光分区。它改善了 LCD 的亮度和控光能力，但并没有改变 LCD 依赖背光的基本结构。」

问题 4：
「OLED 显示黑色真的不耗电吗？」

答案：
「黑色像素可以关闭或接近关闭，因此这部分像素的发光功耗很低。但屏幕控制电路和设备其他部分仍然需要耗电，所以不能理解为整块屏幕完全不耗电。」

问题 5：
「只看 OLED 或 LCD 标签就能判断屏幕质量吗？」

答案：
「不能。同一种面板技术内部仍有巨大差异。分辨率、像素密度、色彩调校、亮度、刷新率、反射率、调光方式和面板均匀性都会影响实际体验。」

九、总结区域

主标题：
「面板技术决定上限，具体产品决定体验」

正文：
「OLED 的核心优势是像素级发光，LCD 的核心优势是成熟、稳定和广泛的产品选择。购买屏幕时，不应只问哪一种技术更先进，而应先明确自己观看什么内容、使用多长时间，以及愿意承担怎样的价格和使用限制。」

总结标签：

- 暗场与响应：OLED 更有优势
- 静态办公与成本：LCD 更加稳妥
- 高端产品：需要逐台比较
- 护眼体验：不能只看面板类型

十、页脚

左侧：
「Display Lab」

说明：
「用更清楚的方式理解显示技术。」

右侧：
「React · Tailwind CSS · Lucide」

底部声明：
「页面内容描述的是常见技术特征，不代表所有具体产品。」

开发要求：

1. 使用语义化 HTML。
2. 页面中只能使用 lucide-react 图标，不要使用 emoji 代替图标。
3. 不要使用外部图片。
4. 不要使用 lorem ipsum。
5. 不要生成虚假的具体参数、实验数据或品牌结论。
6. 将重复内容抽取为数据数组，通过 map 渲染。
7. 使用 TypeScript 时，为数据结构和组件 Props 定义类型。
8. 所有按钮必须是可操作的，不能只是静态装饰。
9. 锚点导航需要平滑滚动，并正确处理固定导航栏偏移。
10. Accordion、Tab、滑块和问答选择器需要支持键盘操作。
11. 页面首次加载时可以加入轻微的淡入和位移动画，但不要过度动画。
12. 不需要后端，不需要登录，不需要数据库。
13. 确保项目可以通过 npm run build。
14. 完成后检查移动端 375px 宽度下是否出现横向滚动。
15. 最终输出修改过的文件列表，并简要说明组件结构。
